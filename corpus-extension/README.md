# Expanded experiment corpus folder

This folder contains original teaching passages for the expanded-corpus
experiment. The selected categories are grammar and opposites. The examples use
different people, objects, and wording from the fixed evaluation cases.

The fixed test suite stays in `evals/`, outside this training folder. The notebook
also checks for exact normalized prompt leakage before it builds the vocabulary or
updates model weights.
