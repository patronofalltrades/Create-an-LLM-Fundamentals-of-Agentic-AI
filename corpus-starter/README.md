# Starter experiment corpus folder

This folder is intentionally empty except for this README. The starter experiment
uses only the supplied classroom corpus. The notebook loader ignores `README.md`,
so no added text enters the starter model's training data.

The fixed language evaluations remain in `evals/`, outside this folder and outside
the expanded experiment's `corpus/` folder.
